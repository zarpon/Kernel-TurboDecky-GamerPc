From f8d30aff508f9d65cbeee058d9c1c422f319ce4d Mon Sep 17 00:00:00 2001
From: Zarpon Kernel Maintainers <noreply@users.noreply.github.com>
Date: Thu, 23 Jul 2026 00:00:00 +0000
Subject: [PATCH] sched: port BORE sched-ext coexistence fix for Liquorix 7.1.3

Port firelzrd/bore-scheduler's
0002-sched-ext-coexistence-fix.patch after the reviewed BORE 6.6.3
Liquorix port.

Liquorix changes the tail context of fair.c, and the normal build treats
-Wmissing-prototypes as an error.  Preserve the upstream helper and add its
internal scheduler declaration so this port applies and compiles without
fuzz.

Upstream-commit: 16bf5baebbb42cdba393c501ba9c2af5f84e4749
Upstream-path: patches/additions/0002-sched-ext-coexistence-fix.patch
Upstream-sha256: cdf138cdb94fcb4e2988bd7d2873a51522fdb7212ec314fde202facaf8210b5c
---
 kernel/sched/fair.c  | 13 +++++++++++++
 kernel/sched/sched.h |  1 +
 2 files changed, 14 insertions(+)

diff --git a/kernel/sched/fair.c b/kernel/sched/fair.c
index b7a2848cc920f2226bfd76b181442a058df07d48..1345bf7ec463b3c49d8b2d0c325cd5f1051fcd9a 100644
--- a/kernel/sched/fair.c
+++ b/kernel/sched/fair.c
@@ -14404,3 +14404,16 @@ __init void init_sched_fair_class(void)
 	zalloc_cpumask_var(&nohz.idle_cpus_mask, GFP_NOWAIT);
 #endif
 }
+
+#ifdef CONFIG_SCHED_BORE
+void reweight_task(struct task_struct *p, int prio)
+{
+	struct sched_entity *se = &p->se;
+	struct cfs_rq *cfs_rq = cfs_rq_of(se);
+	struct load_weight *load = &se->load;
+	unsigned long weight = scale_load(sched_prio_to_weight[prio]);
+
+	reweight_entity(cfs_rq, se, weight);
+	load->inv_weight = sched_prio_to_wmult[prio];
+}
+#endif /* CONFIG_SCHED_BORE */
diff --git a/kernel/sched/sched.h b/kernel/sched/sched.h
index c869fad2ab05885f6c76eb8f5644497228fef737..e0d6e57e09bea3e92741af31f28cccc8bf1fce62 100644
--- a/kernel/sched/sched.h
+++ b/kernel/sched/sched.h
@@ -2244,6 +2244,7 @@ extern void dirty_sched_domain_sysctl(int cpu);
 
 #ifdef CONFIG_SCHED_BORE
 extern void sched_update_min_base_slice(void);
+extern void reweight_task(struct task_struct *p, int prio);
 #else /* !CONFIG_SCHED_BORE */
 extern int sched_update_scaling(void);
 #endif /* CONFIG_SCHED_BORE */
-- 
2.47.0
