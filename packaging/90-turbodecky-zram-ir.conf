# Run after zram0 exists but before zram-generator assigns disksize. This keeps
# ZRAM-IR deterministic even on generators that do not configure secondary
# algorithms themselves.
[Service]
ExecStartPre=/usr/lib/turbodecky/configure-zram-ir %I
